{
  "name": "drishti",
  "version": "1.0.0",
  "private": true,
  "description": "Drishti SIH26038 — clinical frontend (client/) + context-layer API (server/)",
  "scripts": {
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "dev": "concurrently -n api,app -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "eval": "npm --prefix server run eval",
    "ping": "npm --prefix server run ping"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
